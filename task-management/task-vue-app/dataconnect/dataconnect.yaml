specVersion: "v1"
serviceId: "task-vue-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "task-vue-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
